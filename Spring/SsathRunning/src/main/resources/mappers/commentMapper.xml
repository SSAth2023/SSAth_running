<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.board.model.dao.CommentDao">

	<!-- 댓글 상세조회 -->
	<select id="selectComment" parameterType="int">
		SELECT 
			* 
		FROM COMMENT
		WHERE comment_id = #{commentId} 
	</select>
	
	<!-- 모든 댓글 조회 -->
	<select id="selectComments" parameterType="int">
		SELECT * FROM COMMENT
		WHERE map_id = #{mapId}
	</select>
	
	<!-- 댓글 작성 -->
	<insert id="createComment" parameterType="Comment">
		INSERT INTO COMMENT VALUES(#{commentId}, #{mapId}, #{userId}, #{content}, #{comRegDate})
	</insert>
	
	<!-- 댓글 삭제 -->
	<delete id="deleteComment" parameterType="int">
		DELETE FROM COMMENT
		WHERE comment_id = #{commentId}
	</delete>
	
	<!-- 댓글 수정 -->
	<update id="updateComment">
		UPDATE COMMENT SET content = #{content}, reg_date = now()
		WHERE comment_id = #{commentId} AND map_id = #{mapId}
	</update>
	
</mapper>